import { Component } from '@angular/core';
import { TabsModule } from 'primeng/tabs';

@Component({
  selector: 'app-tabs',
  imports: [TabsModule],
  templateUrl: './tabs.html',
  styleUrl: './tabs.scss',
})
export class Tabs {

}
